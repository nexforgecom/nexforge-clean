/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // tambah kalau perlu gambar eksternal
  },
};

export default nextConfig;
